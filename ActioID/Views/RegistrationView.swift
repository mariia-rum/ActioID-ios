import SwiftUI

struct RegistrationView: View {
    var body: some View {
        VStack {
            Text("Registration Coming Soon")
                .font(.title)
                .padding(.top,70)
        }
    }
}
